name: pumped_ada
description: A better breast pump.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2
  flutter_blue: ^0.6.3
  google_sign_in: ^4.1.1

  http: ^0.12.0+4
  infinite_listview: ^1.0.0

  # Required for any firebase plugin
#  firebase_core: ^0.4.0+9

  cloud_firestore: ^0.13.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter


# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  fonts:
    - family: Rufina
      fonts:
        - asset: assets/Rufina-Regular.ttf
        - asset: assets/Rufina-Bold.ttf
          weight: 700
    - family: Rubik
      fonts:
        - asset: assets/Rubik-Regular.ttf
        - asset: assets/Rubik-Light.ttf
          weight: 300
        - asset: assets/Rubik-Medium.ttf
          weight: 700
